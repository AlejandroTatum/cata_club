# 1374 — FAQ: one authoritative schedule update path

## Objective
Complete the remaining #1374 acceptance criterion on top of the already-merged
chatbot removal and FAQ rework: schedules must have one authoritative update
path. `/ayuda` currently renders a static `horarios[]` snapshot from
`conocimiento_club.json` while the landing renders the dynamic public catalog
(`GET /api/schedules` → backend `/asistencias/horarios-publicos`). The dynamic
catalog is the authoritative source; the static list is a second copy nobody
synchronizes.

Verified on `feat/1374-faq` @ 5719ac0: `python3 scripts/diagnostico_horarios.py`
reports `static_schedule_authority: 1` ("Todavía manda en: ... la página /ayuda
(faq-content.ts:88, FAQ_SCHEDULES)") with a live catalog of 5 categorías.

## Scope
- `/ayuda` renders its schedule table from `GET /api/schedules`, reusing the
  landing's `mapPublicSchedules` mapper (single mapping authority). Accessible
  loading / error / empty / table states, in the page's usted register.
- `FAQ_SCHEDULES` and the `FaqSchedule` interface are removed from
  `faq-content.ts`; the page no longer reads `horarios` from the mirror.
- `conocimiento_club.json` `horarios[]` becomes `[]` (backend serializer keeps
  working untouched: section key stays present, list empty). Regenerate the two
  derived artifacts with the canonical `make sync-knowledge`.
- Guards adjusted, only where the migration makes them lie:
  - `backend/tests/test_conocimiento_club.py`: static-list tests become
    "the static list stays empty / the prompt carries no static schedules".
  - `frontend/.../knowledge-parity.test.tsx`: schedule parity vs prompt is
    replaced by a "the snapshot carries no schedule lines" regression guard.
  - `frontend/.../faq-content.test.ts`: `FAQ_SCHEDULES` block removed.
  - `frontend/.../AyudaPage.test.tsx`: schedule table tests mock the catalog
    fetch and cover rows/loading/error/empty states.
  - `scripts/diagnostico_horarios.py` `_SUPERFICIES_ESTATICAS` + finding text
    now describe the completed migration; detection logic unchanged.
  - `tests/test_diagnostico_horarios.py` updated accordingly (live file now
    has 0 static entries).
  - `docs/operations/diagnostico-horarios.md` `static_schedule_authority`
    section documents the completed migration and the regression-guard role.
- `frontend/tests/e2e/site-navigation.spec.ts`: `/ayuda` reachable, primary
  content renders, schedule section resolves to table or graceful status.

## Non-goals
- No chatbot resurrection (removed on main; owned by refactor/remove-chatbot-ui).
- No backend schedule contract change; no `/api/schedules` BFF change; no
  landing behavior change (`schedule-data.ts` reused, not edited).
- No delivery: no commit, push, PR, or auto-merge — per-cut approval pending.

## Constraints
- Worktree `/home/alejo/devwork/apps/cata_club-worktrees/gentleman-1374-faq`,
  branch `feat/1374-faq` from origin/main (5719ac0).
- Validation: frontend + root tests only. No db-test, no backend DB.
- Keep scope small; preserve knowledge-parity architecture (DOM vs snapshot
  bytes) for FAQ/club content.

## TDD and routing
- TDD mode: not activated by the parent task; tests updated alongside behavior.
- Single writer (this worktree); delegated implementation.

## Tasks

- [x] T1 — `/ayuda` schedule table consumes the dynamic catalog
  (`page.tsx`, `faq-content.ts`), accessible states, usted copy.
- [x] T2 — Static schedule list emptied in canonical knowledge;
  `make sync-knowledge` regenerated mirror + prompt snapshot byte-exactly
  (canonical 4994-char snapshot; `--verificar` synchronized).
- [x] T3 — Knowledge guards updated (backend test, parity test,
  faq-content test) to enforce the migration end-state.
- [x] T4 — `AyudaPage.test.tsx`: catalog-driven table tests with fetch mock
  (rows, multi-slot, missing audience, loading, error, empty).
- [x] T5 — Schedule diagnostic guard migrated (script text, root tests, ops doc).
- [x] T6 — `/ayuda` navigation/content e2e added to site-navigation.spec.ts.
- [x] T7 — Focused validation executed; one deterministic test-text failure
  found and fixed (assertion collided with narrative text, not behavior);
  all gates green afterwards.

## Correction cut C2 — /ayuda is the FAQ, whole (owner preview review)

Owner reviewed the live preview and corrected the product: /ayuda keeps ONLY
the four FAQ accordion groups. Removed surfaces: the schedule table (even the
dynamic one) and the club-facts / escape-hatch informational blocks. The
"¿Cuáles son los horarios?" answer must DIRECT to the landing's authoritative
live section (/#horarios anchor, already shipped) with a working link, and
must not restate schedules, prices, or other unverifiable facts.

Constraints discovered:
- `tests/test_glossary_contract.py` (outside edit surfaces) requires the
  exact fragment "Horarios de entrenamiento" in the prompt — the rewritten
  answer keeps it as the glossary's canonical concept, truthfully.
- `Accordion.answer` already accepts ReactNode: the link renders inside the
  answer with zero component changes; link text is a verbatim substring of
  the canonical answer so the DOM-vs-prompt parity guard keeps holding.
- The landing already ships the anchor (`id="horarios"`); no landing change.

- [x] C2.1 — Rewrite the horarios FAQ answer in canonical knowledge
  (preserve carnet fact; drop "días y horas fijos" claim); sync artifacts
  (snapshot 5014 chars; glossary fragment preserved; mirror byte-identical).
- [x] C2.2 — Page becomes FAQ-only: schedule section, El club, escape hatch
  removed; site link rendered inside the horarios answer (link text is a
  verbatim substring of the canonical answer, so parity holds byte-wise).
- [x] C2.3 — Tests/contracts updated: parity (club facts guarded by absence,
  fetch stub dropped), AyudaPage (FAQ-only guards + link + no-times/no-price
  answer sweep), e2e (accordion → /#horarios click-through), diagnostico
  narrative de-staled (script + ops doc; "catálogo dinámico" phrase kept for
  the root-test assertion).
- [x] C2.4 — Focused validation green (ayuda 35, root diagnostico 46,
  type-check, lint, offline knowledge contract replica, e2e site-navigation
  3/3); lane `make pre-pr LANE=frontend` one run, 203 e2e passed, tracked
  tree identical pre/post. Incident: the lane's `pnpm build` clobbered the
  shared `.next` of the running preview dev server (HTTP 500); restored the
  preview with the parent's own launcher script (same command/port/env) —
  verified 200 + corrected content. No source changes from the incident.

## Correction cut C3 — owner-approved 16-answer copy (no delivery)

Owner approved exactly 16 concise answers (usted neutral; link text narrowed
to only "Horarios de la página principal" → /#horarios). Questions and four
groups unchanged; answers replaced verbatim; nothing else invented.

Known copy conflicts to REPORT (gates outside allowed surfaces):
- `tests/test_glossary_contract.py` USOS_BACKEND requires prompt fragment
  "Horarios de entrenamiento" — the only source was the old horarios answer.
- USOS_FRONTEND requires mirror fragment "valor de cada plan" — the only
  source was the old mensualidad answer.
Both need either an owner-approved contract update or new canonical wording;
not invented here.

- [x] C3.1 — Canonical JSON: 16 respuestas replaced verbatim; sync artifacts.
- [x] C3.2 — page.tsx ANSWER_LINKS link text narrowed; tests updated
  (AyudaPage link name; faq-content medical-record locks; backend
  exact-fact tests).
- [x] C3.3 — Focused tests + guards (glossary/ortografia root run to verify
  the conflicts), preview 3394 live with new copy; report URL + checks.

## Evidence

- `pnpm vitest run src/app/ayuda`: 4 files / 41 tests passed.
- `pnpm vitest run .../landing schedule-data + LandingPage + api/schedules`:
  3 files / 92 tests passed.
- `pnpm type-check`: OK. `pnpm lint`: only pre-existing warning in
  `src/app/sponsors/page.tsx` (untouched).
- `make test-diagnostico-horarios`: 46 passed.
- `make sync-knowledge` + `--verificar`: synchronized; mirror byte-identical
  (`cmp`).
- Offline backend knowledge contract replica (uv run, stdlib only):
  `horarios == []`, snapshot fresh, no static schedule lines.
- `pnpm exec playwright test tests/e2e/site-navigation.spec.ts`: 3 passed
  (branch's own production build, port 3390).
- Full frontend unit suite (`pnpm test`): 301 files / 5073 tests passed.
- `python3 scripts/diagnostico_horarios.py` (live):
  `static_schedule_authority: 0`.

## Skipped gates (no delivery pending)

- Backend pytest suite (incl. the updated `test_conocimiento_club.py`):
  conftest hard-requires `TEST_DATABASE_URL` → shared `db-test`, excluded by
  the parent task. Compensated with the offline contract replica + sync
  verification above.
- Remaining root `tests/` checks (`make test-root`) and the rest of the e2e
  suite: unaffected surfaces; full lanes are pre-PR gates and no push/PR is
  authorized yet.
- `revision_drift: 1` in the live diagnostic is environmental (the local
  stack on :3000 serves a different build SHA), pre-existing and unrelated.

## Delivery

None. No commit, push, PR, or auto-merge — per-cut approval pending.
Working tree holds the 13 modified surfaces + this tracker file.

## Pre-PR lane (parent-authorized single run)

- Pre-run git status recorded (13 surfaces + tracker, HEAD 5719ac0, no stashes).
- `make pre-pr LANE=frontend` — one run, no reruns: guard-secrets OK,
  `pnpm audit --audit-level=high` OK, `pnpm type-check` OK, `pnpm lint` OK,
  `pnpm run test:coverage` OK, `pnpm build` OK, `pnpm exec playwright test`
  203 passed (3.4m, chromium + mobile-chromium projects; live/mobile `.live`
  excluded by project testIgnore). Lane exited clean; any earlier phase
  failure would have aborted before Playwright (make sequential semantics).
- Post-run git status identical to pre-run: no tracked file modified; lane
  artifacts confined to gitignored paths (.next, coverage, playwright-report,
  test-results). No db-test involved (lane recipe has no DB step).
- Note for the integrator: `origin/main` advanced 1 commit after this branch
  was cut (5719ac0); branch reports [detrás 1] against its stale tracking ref.

### C3 evidence
- Canonical JSON: 16/16 respuestas replaced verbatim (mapped by exact
  pregunta); `make sync-knowledge` + `check-knowledge` synchronized; mirror
  byte-identical (cmp); snapshot 3466 chars.
- Ayuda vitest: 35 passed. type-check OK; lint clean. e2e
  site-navigation 3/3 (link name narrowed). Offline knowledge contract
  replica OK (16 Q, narrowed link, no times/prices restated).
- Root guards run offline (uv pytest): `test_copy_ortografia_contract` PASS;
  `test_glossary_contract` 2 EXPECTED FAILURES = the reported copy conflicts
  ("Horarios de entrenamiento" in prompt, "valor de cada plan" in mirror) —
  gates outside allowed surfaces; not invented around.
- Preview 3394: HTTP 200; renders `en la sección <a href="/#horarios">Horarios
  de la página principal</a>.` inside the answer; club-facts/old copy absent.
  (Transient: playwright's managed build touched shared `.next` mid-curl;
  dev server self-recovered — content verified after.)

### Gate fixture fix (parent-authorized, no user scope decision)
The two reported copy conflicts were stale substring fixtures from the old
FAQ, not a copy problem: owner-approved copy stands. Updated ONLY the two
representative fragments in `tests/test_glossary_contract.py` —
horario_entrenamiento → "horarios vigentes" (exact lowercase case verified
against the prompt), tipo_membresia → "valor de su plan" — with the adjacent
comment now describing existence-only checks against approved FAQ copy.
Every other gate row, provenance hash, and safeguard untouched.

Evidence: `make check-knowledge` synchronized; `pytest
tests/test_glossary_contract.py` 14 passed (both gate tests green, mirror
byte-equality and prohibited-variant sweep still exercised); FAQ focused
vitest 35 passed; preview 3394 live (HTTP 200). No other path changed; no
delivery.

### Approval + RDD switch (owner-authorized, no delivery)
- `gh issue edit 1374 --add-label status:approved` applied; authoritative
  confirm: labels ["status:approved","type:feature"], state OPEN.
- `gentle-ai review mode enable --scope clone` executed exactly; native CLI
  model is "any off wins" — clone scope may only disable, never require — so
  with global=on the authoritative `review mode status` reports
  effective=on (source: global, revision sha256:6b3c9ae8...), clone_local
  structurally unset. Global untouched; nothing disabled.
- Final frontend lane run exactly once: guard-secrets, audit, type-check,
  lint, test:coverage, build all green by make semantics; e2e 203 passed
  (3.3m). Tracked tree identical pre/post (15 entries: 14 modified + this
  tracker); zero drift. Preview restored after the known build/dev shared-
  `.next` collision (launcher re-run, HTTP 200, approved copy served).
- Stopped before delivery: candidate awaits parent-controlled native review.

### Slicing attempt (parent-authorized, stopped cleanly — NO commit made)
One honest slicing pass over the 774-line candidate (280+/494−, 14 files):
no cohesive whole-file split yields a ≤400-line independently-green first
slice. Executed evidence in a scratch worktree from origin/main (0dc1830):
- Knowledge/contract layer alone (9 files, 329 lines): type-check FAILS —
  base `faq-content.ts` reads `horarios` from the emptied mirror
  (TS2339 'horas' on 'never'); with final `faq-content.ts` (+34 = 363) base
  `page.tsx` fails instead (TS: `FAQ_SCHEDULES` export gone) and 25 base
  tests fall (AyudaPage import + parity rows). The mirror sync-lock, the
  FAQ_SCHEDULES import chain, and the parity↔snapshot↔page triangle make the
  candidate atomic: minimum compilable+green unit = the whole 774.
- Scratch worktree/branch removed losslessly (byte-copies only); FAQ
  worktree untouched (15 entries, HEAD 5719ac0, no commits) and preview
  live (HTTP 200).
Recommendation: single work-unit commit of the whole candidate with
`size:exception` (atomicity evidence above) for parent-controlled native
review. Awaiting parent decision; no commit/push/PR/RDD CLI run.

### Delivery decision (owner): ONE PR per issue — size:exception granted
The owner chose one PR per issue for #1374 and granted `size:exception` for
the indivisible 774-line candidate. Atomicity evidence (recorded above):
knowledge mirror sync-lock (byte equality), FAQ_SCHEDULES import chain
(TS2339 both directions), and the parity↔snapshot↔page triangle — the
minimum compilable+green unit is the whole change. Slicing attempt: see
"Slicing attempt" section (scratch worktree proved it, removed losslessly).
Branch updated safely from origin/main (0dc1830, gallery work) with the e2e
spec stashed for the fast-forward and restored on top (main removed the
Logros nav entries; the #1374 describe appends disjointly). Commit: this
commit — one conventional work unit for the complete #1374 candidate
(including this tracker), reviewed by native RDD before any delivery.
